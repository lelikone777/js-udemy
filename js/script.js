"use strict";

function showFirstMessage(text) {
  console.log(text);
}

showFirstMessage('hello world');

function  calc(a, b) {
  return (a + b);
}

function ret() {
  let num = 50;
  return num;
}

const calv = (a, b) => a + b;